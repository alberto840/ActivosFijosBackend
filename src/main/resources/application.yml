
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Activo_DB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true
server:
  port: 8080
